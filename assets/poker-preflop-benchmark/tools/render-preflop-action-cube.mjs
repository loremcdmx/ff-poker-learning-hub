#!/usr/bin/env node

throw new Error(
  "render-preflop-action-cube.mjs is disabled because it left the frozen time/user placeholders unresolved. "
  + "Use prepare-source-run.mjs for the publication run or render-action-cube-query.mjs for one explicit half-open time shard.",
);
